package java_Book_Chapter3;

import java.util.Scanner;

enum MovieRating {E, A, B}

public class Study 
{
	public static void main(String[] args) 
	{
		/*
		if (true){
			System.out.println("truth");
		}
		else {
			System.out.println("false");
		}
		
		String txt = "Hello";
        System.out.println(txt.toUpperCase());
        
        String firstName = "John ";
        String lastName = "Doe";
        
        int goals = 20;
        
        if (goals > 10)
        	System.out.println("Wow");
        else
        	System.out.println("Oh Well");
        
		
		int goals = 11, errors = 0;
		if ((goals > 10) && (errors == 0))
			System.out.println("Wow");
		else
			System.out.println("Oh Well");
		
		
		double salary = 100000, deductions = 99000, net;
		if (salary >= deductions)
		{
			System.out.println("OK");
			net = salary - deductions;
		}
		else
			System.out.println("No Way");
		
		
		int speed, visibility;
		if ((speed > 25) && (visibility < 20) )
		{
			speed = 25;
			System.out.println("Caution");
		}
		
		
		double salary, bonus;
		final double MIN_SALARY, MIN_BONUS;
		if ((salary >= MIN_SALARY) || (bonus >= MIN_BONUS))
			System.out.println("OK");
		else
			System.out.println("Too low");
		
		
		String nextWord = "near";
		System.out.println(nextWord);
		String upperCase = nextWord.toUpperCase();
		System.out.println(upperCase);
		if (upperCase.compareTo("N") < 0)
			System.out.println("First half of the alphabet.");
		else
			System.out.println("Second half of the alphabet.");
		
		
		Scanner keyboard = new Scanner(System.in);
		int hoursWorked;
		double payRate, pay = 0;
		
		System.out.println("Enter your hours for last week:");
		hoursWorked = keyboard.nextInt();
		System.out.println("Enter your pay rate:");
		payRate = keyboard.nextDouble();
		
		if (hoursWorked < 0 || payRate < 0)
			System.out.println("You entered an invalid entry!");
			System.exit(0);
		
		if (hoursWorked <= 40)
			pay = hoursWorked * payRate;
		else if (hoursWorked > 40)
			pay = 40 * payRate + 1.5 * payRate * (hoursWorked - 40);
		
		pay = (hoursWorked <= 40) ? (hoursWorked * payRate) : 40 * payRate + 1.5 * payRate * (hoursWorked - 40);
		
		System.out.println("Your pay for last week is: $" + pay);
		
		
		int a = 127;
		byte b = (byte)a; // explicit casting
		System.out.println(b);
		
		
		// The enum data type was defined before main
		MovieRating rating;
		rating = MovieRating.E;
		
		switch (rating)
		{
		case E: //Excellent
			System.out.println("You must see this movie!");
			break;
		case A: //Average
			System.out.println("This movie is OK, but not great.");
			break;
		case B: //Bad
			System.out.println("Skip it!");
			break;
		default:
			System.out.println("Something is wrong.");
		*/
		
		
		
		
		
		
		
	}

}
